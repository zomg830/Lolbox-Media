{
  "scripts": {
    "start": "cd ./server && npm start",
    "install": "cd ./server && npm install",
    "build": "cd ./server && npm run build"
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "nodemon": "^1.18.7"
  }
}
